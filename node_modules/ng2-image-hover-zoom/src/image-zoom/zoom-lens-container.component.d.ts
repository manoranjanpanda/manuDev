import { ElementRef } from '@angular/core';
export declare class ZoomLensContainer {
    private _elementRef;
    el: HTMLElement;
    constructor(_elementRef: ElementRef);
    setOptions(options: any): void;
    setVisibility(visible: boolean): void;
}
