import { ElementRef, Renderer, OnInit } from '@angular/core';
export declare class ZoomableDirective implements OnInit {
    private el;
    private renderer;
    hoverView: HTMLElement;
    peepView: HTMLElement;
    zoomView: HTMLElement;
    imgRect: ClientRect;
    listeners: Function[];
    hoverViewPosition: {
        width: number;
        height: number;
        top: number;
        left: number;
    };
    options: {
        hoverView: {
            background: string;
            position: string;
            zIndex: string;
        };
        peepView: {
            borderColor: string;
            borderWidth: string;
            borderStyle: string;
            cursor: string;
            position: string;
        };
        zoomView: {
            position: string;
            zIndex: string;
        };
        settings: {
            zoom: number;
            gap: number;
        };
    };
    customOptions?: {
        hoverView?: {};
        peepView?: {};
        zoomView?: {};
        settings?: {};
    };
    sourceImage?: string;
    constructor(el: ElementRef, renderer: Renderer);
    onMouseEnter(): void;
    ngOnInit(): void;
    /**
     * Creates element that acts as wrapper for peep box
     */
    private createHoverView();
    /**
     * Creates peep view element that moves with the mouse over the image
     */
    private createPeepView();
    /**
     * Creates the zoom view element
     */
    private createZoomView();
    private assignHoverListeners();
    private styleHoverView();
    private stylePeepView();
    private styleZoomView();
    private updateHoverViewPosition();
    private positionHoverView();
    private getPeepViewDimensions();
    private getPeepViewCoords(event);
    private positionPeepView(event);
    private positionZoomView();
    private positionZoomBackground(event);
    private destroyViews();
}
